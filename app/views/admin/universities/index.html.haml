%h1 Universities

.text-center
  = link_to "New University", new_admin_university_path, class: "btn btn-default btn-lg"

%table.table
  %thead
    %tr
      %th ID
      %th Name
      %th Slug
      %th

  %tbody
    - @universities.each do |university|
      %tr
        %td= university.id
        %td= university.name
        %td= university.slug
        %td= link_to "Edit", edit_admin_university_path(university), class: "btn btn-default"
        %td= link_to "Edit Courses", admin_university_courses_path(university), class: "btn btn-default"